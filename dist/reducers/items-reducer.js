//import * as types from '../constants/action-types'

/**
 * Items reducer
 *
 * @param state - items List
 * @param action
 * @returns state
 */
const items = (state, action) => {
    switch (action.type) {

        /** default */
        default:
            return state;
    }
};

export default items

